{
  "name": "app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "vite": "^4.4.5",
    "vite-plugin-restart": "^0.3.1"
  }
}
